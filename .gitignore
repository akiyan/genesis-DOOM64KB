# ビルド成果物
/out/
/tmp/

# SGDK 本体（symlink / 各環境で用意。詳細は README）
/vendor/sgdk

# 自動生成物（build.sh が scripts/doom64.wad から再生成）
/scripts/doom64_aligned.wad
/scripts/__pycache__/
/src/doom64ng.h
/src/gen_pal.h
/src/gen_sfx.h

# SFX 音源 WAD（リポジトリには含めない。各自 scripts/ に配置）
/scripts/doom1.wad

# BGM VGM（リポジトリには含めない。各自 res/ に配置）
/res/*.vgm

# rescomp 生成物（res/*.res と各自配置した *.vgm から再生成）
/res/*.h
/res/*.rs
/res/*.d
._*

# エディタ等
*.o
*.lst
*.bin
.DS_Store
